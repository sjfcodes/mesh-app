{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "AWS_PROFILE=mesh-app-deployer NODE_OPTIONS=--experimental-vm-modules jest --watchAll=false",
    "test:it": "test/integration/run.sh",
    "test:it:api": "export USE_API_GATEWAY=true && npm run test:it",
    "test:it:local": "npm run test:it",
    "format": "prettier --write \"lambdas/**/*.{js,mjs}\"",
    "deploy:all": "npm run format && cd lambdas && npm run deploy:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.2"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "jest": "^29.3.1",
    "jest-runner-groups": "^2.2.0",
    "prettier": "^2.7.1"
  }
}
