{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "export AWS_PROFILE=mesh-app-deployer && NODE_OPTIONS=--experimental-vm-modules npx jest ${TEST_FILE} --watchAll",
    "test:it:local": "export TEST_FILE=test/integration/awsLambdaDynamoDb.test.js && npm run test",
    "test:it:api": "export TEST_FILE=test/integration/awsApiGateway.test.js && npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.2"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "jest": "^29.3.1"
  }
}
