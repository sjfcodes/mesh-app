import Loader from '../Loader/Loader';
import './style.scss';

const SectionLoader = () => {
  return (
    <div className="ma-section-loader">
      <Loader />
    </div>
  );
};

export default SectionLoader;
