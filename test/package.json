{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test:it:local": "export AWS_PROFILE=mesh-app-deployer && NODE_OPTIONS=--experimental-vm-modules npx jest integration/awsLambdaDynamoDb.test.js --watchAll ",
    "test:it:api": "export AWS_PROFILE=mesh-app-deployer && NODE_OPTIONS=--experimental-vm-modules npx jest integration/awsApiGateway.test.js --watchAll"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.2"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "jest": "^29.3.1"
  }
}
